/**  BigInt represents an integer that can be many digits long, stored
 *   as an ArrayList of digits.
 *   
 *   @author APCS
 *   @version January 2018
 */

import java.util.ArrayList;

public class BigInt
{
    private ArrayList<Integer> num;
    
    /**  Constructor takes a standard int and stores it as a BigInt
     *   Precondition:  n > 0
     * 
     * @param n  the integer to store as a BigInt
     */
    public BigInt (int n)
    {
    }
    
    /**  Constructor takes a String and stores it as a BigInt
     *   Precondition:  str is a String containing only digits
     * 
     * @param str  the String to store as a BigInt
     */
    public BigInt (String str)
    {
    }
    
    /**  Writes a BigInt as a regular number       */
    public String toString()
    {
        return "";
    }
    
    /** Multiplies a BigInt by a standard int        
      *
      * @param   n  the int to multiply by the BigInt
       */
    public void multiply (int n)
    {
    }
 
      
}